# Flask and Core
flask>=2.3.0
python-dotenv>=1.0.0
gunicorn>=21.0.0

# Document Generation
reportlab>=4.0.0
python-docx>=0.8.11
python-pptx>=0.6.21

# Utilities
pillow>=10.0.0

# Note: For local development with Ollama, install: pip install ollama
# Note: Image generation requires torch, diffusers, transformers, accelerate
#       These are not included by default due to size (~2GB+)
